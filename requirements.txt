# FastAPI and dependencies
fastapi>=0.135.0
uvicorn>=0.32.0
python-multipart>=0.0.12
asgi-correlation-id>=5.0.0
structlog>=24.4.0

# LangChain and LangGraph
langchain>=1.2.0
langchain-openai>=1.1.0
langchain-community>=0.3.0
langgraph>=1.2.0
langchain-core>=1.4.0
numpy>=2.0
tiktoken>=0.12.0
# DuckDuckGo 搜索工具运行时依赖（langchain-community 按需加载）
ddgs>=9.0.0
# 图表生成工具 generate_chart
matplotlib>=3.10.0
# 内置 CLI（python -m app.cli.init_tools）
click>=8.3.0

# Long-term memory (mem0 + pgvector)
mem0ai>=0.1.0

# RAG: document parsing + chunking + embedding（local provider 用 bge 本地模型，无需 API）
pypdf>=4.0.0
langchain-text-splitters>=1.0.0
sentence-transformers>=3.0.0

# Database
asyncpg>=0.31.0
psycopg2-binary>=2.9.0
psycopg[binary,pool]>=3.2.0
sqlmodel>=0.0.14
sqlalchemy>=2.0.0
pgvector>=0.3.0
alembic>=1.18.0

# Authentication
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# Rate limiting
slowapi>=0.1.9
limits>=3.5.0

# Redis/Valkey + RQ task queue
redis>=5.0.0
rq>=1.16.0

# Environment variables
python-dotenv>=1.0.0

# Pydantic
pydantic>=2.8.0

# Retry logic
tenacity>=8.5.0

# Observability (self-hosted Langfuse, deploy: langfuse/docker-compose.yml)
langfuse>=4.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
pytest-cov>=5.0.0
