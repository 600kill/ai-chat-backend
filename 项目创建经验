1. 核心本质

docker-compose.yml是一个 声明式配置文件。它用 YAML 格式描述了一个或多个 Docker 容器的 期望运行状态。

2. 解决的问题

Docker 命令行工具（docker run）是 命令式​ 的。你通过一长串参数告诉 Docker “现在做什么”（例如：运行一个容器，设置环境变量，映射端口）。这种方式在复杂、多容器的应用场景下，存在三个主要问题：

不可重复：命令参数长且复杂，难以记忆和准确复现。

缺乏关系定义：多个容器之间如果有依赖关系（如Web应用依赖数据库），你需要手动管理它们的启动顺序和网络连接。

状态管理困难：启动、停止、清理一组容器需要执行多条命令。

3. 工作原理

Docker Compose 是一个工具，它读取 docker-compose.yml文件，解析其中声明的状态，然后通过 Docker API 自动执行一系列 docker run等命令，使现实状态与文件描述的状态一致。
整体流程：从 Docker 镜像到可用的数据库服务

第一阶段：镜像（Image）阶段
核心：镜像是静态的模板文件，包含运行所需的一切
镜像来源：pgvector/pgvector:pg16是别人打包好的 PostgreSQL 16 镜像，已包含 pgvector 扩展
镜像位置：存在你本地的 Docker 镜像仓库中（通过 docker images可见）
镜像本质：一个只读的、分层的文件系统模板，像游戏安装包
第二阶段：容器（Container）阶段
核心：容器是镜像的运行实例，是动态的、隔离的进程
创建容器：通过 docker run命令，基于镜像创建一个可写的容器层
容器启动：容器内启动 PostgreSQL 数据库进程
网络映射：容器内部端口5432映射到你电脑的5432端口
生命周期：容器运行、可停止、可删除
第三阶段：数据持久化阶段
核心：解决容器数据易失问题
默认情况：容器内的数据存在容器的可写层，容器删除则数据丢失
持久化方案：通过 Docker 卷（Volume）将数据保存到宿主机
卷本质：宿主机上的目录，挂载到容器内指定路径
数据安全：即使容器删除，卷中数据保留
第四阶段：外部连接阶段
核心：从外部访问容器内的数据库
网络通道：通过 localhost:5432连接到容器内的 PostgreSQL
连接方式：
命令行：psql -h localhost -p 5432
Python：psycopg2.connect(host="localhost")
应用：任何支持 PostgreSQL 的客户端
认证：使用容器启动时设置的用户名/密码
第五阶段：数据库操作阶段
核心：在运行的数据库上进行操作
DDL操作：创建表、修改表结构（通过 Alembic 管理）
DML操作：插入、查询、更新、删除数据
扩展功能：使用 pgvector 进行向量计算
管理操作：备份、恢复、监控
第六阶段：开发迭代阶段
核心：应用开发中的数据模式演进
模型定义：在代码中定义数据库表结构（SQLModel）
迁移生成：Alembic 对比模型与数据库，生成迁移脚本
迁移应用：执行迁移脚本，安全地修改数据库结构
版本控制：迁移脚本纳入 Git 版本管
第七阶段：清理阶段
核心：资源管理和清理
停止容器：停止数据库进程
删除容器：删除容器实例，释放资源
清理卷：删除不再需要的数据卷
删除镜像：清理不再使用的镜像文件


# Alembic+SQLModel+Docker PostgreSQL 实操总结
## 一、基础流程
1. 安装项目所需全部依赖包
2. 终端执行 `alembic init alembic`，一键自动生成迁移工具完整目录与配置文件，无需手动创建文件夹和文件
3. 自行编写数据表模型代码，定义表字段、主键、外键等结构
4. 修改工具生成的配置文件，调整内部参数，让工具和自定义模型、数据库环境相互匹配
5. 启动Docker编排的数据库容器，确认数据库正常运行
6. 执行迁移相关指令，完成模型同步建表
7. 进入数据库核对表结构，确认搭建完成

## 二、核心文件作用
1. **alembic.ini**：存放数据库连接地址、账号密码，必须改为Docker数据库真实连接地址
2. **alembic/env.py**：导入自定义数据表模型，让迁移工具识别项目表结构，适配数据库连接规则
3. 版本目录：存放每一次表结构改动记录，支持版本回退

## 三、完整操作步骤
1. 编写自定义数据表模型，代码内添加 `table=True`
2. 命令生成迁移工具整套目录结构
3. 修改 `alembic.ini`，替换默认数据库链接
4. 修改 `env.py`，导入所有自定义模型，绑定模型元数据，添加适配参数
5. 执行 `docker-compose up -d` 启动数据库容器，`docker ps` 核查运行状态
6. 执行 `alembic revision --autogenerate -m "备注"`，自动比对模型生成迁移脚本
7. 在生成的迁移脚本首行添加 `import sqlmodel`
8. 执行 `alembic upgrade head`，将模型结构同步写入数据库
9. 执行 `alembic current` 查看迁移版本，进入数据库用 `\dt` 查看数据表

## 四、实操踩坑整理
1. 数据库驱动加载失败、链接解析错误
    原因：未修改默认数据库链接
    解决：填写Docker映射后的标准PostgreSQL链接
2. 检测不到数据表无结构变动
    原因：env.py未导入自定义模型
    解决：在配置文件顶部引入全部模型
3. 数据库连接成功后自动断开
    原因：默认配置不兼容容器数据库
    解决：env.py内添加连接超时适配参数
4. 执行迁移提示sqlmodel未定义
    原因：自动生成脚本缺少依赖导入
    解决：迁移文件头部手动导入sqlmodel
5. 始终无法连接数据库
    原因：Docker数据库容器未启动
    解决：先启动容器，再执行所有数据库操作

## 五、常用指令
```bash
alembic revision --autogenerate -m "备注"  # 生成迁移文件
alembic upgrade head                      # 执行迁移建表
alembic current                           # 查看当前迁移版本
docker-compose up -d                      # 启动数据库容器
docker exec -it 容器名 psql -U postgres -d postgres  # 进入数据库
\dt                                       # 查看所有数据表
```
## 六、核心要点
1. 工具仅负责生成基础框架，所有个性化配置、模型对接都需要手动修改参数完成匹配
2. 数据库结构改动只修改本地模型文件，依靠迁移指令同步，不直接手动操作数据库
3. 容器数据库必须优先启动服务，再进行迁移、连接等所有相关操作
1、项目核心架构（一句话）
一、必学刚需
表结构迭代迁移
改模型增删改字段，重新生成 + 执行迁移，线上项目日常必用，不会就没法迭代业务
迁移回滚
上线出错一键撤回表结构变更，项目排错刚需，必备保命操作
标准 CRUD 整套写法
和 MySQL 逻辑一致，只是 ORM 语法，业务核心根基，必须自己熟练写
单条增、批量增
全查、条件精准查询
数据修改、删除
全局数据库引擎 + 会话封装
项目统一管理连接，不用到处写链接地址，正式项目必备架构写法
外键基础关联使用
你已写外键字段，必会联表查询，会话、用户表联动业务离不开
FastAPI 提供接口，LangGraph 跑 Agent，pgvector 存记忆，Alembic 管表结构。

二、4 个必背问题（极简版）
✅ 1. 架构什么样？

背景：要做可落地的 AI 应用，不能只调 API。

设计：FastAPI + LangGraph + PostgreSQL(pgvector)。

代码：

main.py：启动 API

services/agent_service.py：LangGraph Agent

services/memory_service.py：pgvector 检索

✅ 2. 怎么解决记不住？

背景：上下文有限，Token 贵。

设计：RAG + 长期记忆（向量检索）。

代码：

long-term-memory.md

models/：User/Session/Thread

mem0管向量表，Alembic 不管

✅ 3. 怎么保证稳定性？

背景：AI 容易费钱、难排查。

设计：可观测性（Langfuse + Prometheus）。

代码：

observability.md

middleware/logging.py：request_id

/metrics给 Prometheus 用

✅ 4. 数据库怎么管？

背景：表结构经常变，不能 create_all。

设计：Docker + Alembic 版本化管理。

代码：

database.md

alembic/env.py：排除外部表

docker-compose.yml：pgvector + 持久化

三、明天翻代码的最小路径（只看 5 处）

1️⃣ main.py→ API 怎么启动

2️⃣ app/services/→ Agent / 向量检索 / LLM 调用

3️⃣ app/models/→ 表结构

4️⃣ database.md→ Alembic + pgvector

5️⃣ observability.md→ 监控与日志

四、面试收尾一句话（必背）

“我这不是 Demo，是一个工程化的 AI 应用，数据库、Agent、记忆、监控都是按生产标准设计的。”